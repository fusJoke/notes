抽象类和接口的区别
抽象类的特点

+ 抽象类不允许被实例化，只允许被继承
+ 抽象类可以包含属性和方法
+ 子类继承抽象类，必须实现所有的抽象方法

接口的特点

+ 接口不含有属性
+ 接口只声明方法，不包括对方法的实现
+ 类的实现接口时候，必须实现所有的方法



抽象类和接口解决了什么编程上的问题？

抽象类的作用：

+ 代码复用
+ 强制要求子类重写方法，减少出错

接口的作用：
主要的用在解耦上面。接口是对行为一种抽象，相当于一组契约一组协议。