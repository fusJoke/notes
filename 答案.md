1. 在 foreach中  $item已经被赋值；防止后面代码勿用，重新注销变量

2. 在可重复读级别下面的快照读，相同的查询sql只会打一次快照数据；所以time2，time4，time6的快照数据都是一样的。又因为time2>time3，事务B对数据的更改的产生的unlog版本链不被事务A可见；所以三次查询都是100

3. 自增id是聚簇索引，是排序好且和磁盘io顺序一致；因为是自增id所以在插数据的时候，会自动插满一页数据页；且不会因为id不连续，自动分裂多个数据页来存储；就是我一个数据页里面id=8接着id=16；当我插入id=10的时候。因为数据在数据页里面是在usercode部分用链表连接起来的，这里会有个插入链表的代价；还有可能触发页分裂，要将部分数据复制到新的数据页上面，来保证整个索引数据都是有序的
   uuid一般用在解决分库分表之后的分布式主键id问题，它可以保值全局id唯一；但是它是随机io，性能较差

4. 一致性hash

   我只知道用在分布式缓存上面；让对数据的缓存均匀分散到各个节点；但是如果节点数量太少的话；还是会发生倾斜；可以引入虚拟节点；一个实际节点对应多个虚拟节点；来解决问题

5. 分布式锁方案

   我只知道redis实现。锁可以用一个变量来表示。变量值为0时，表示没有线程获取锁。变量值为1时，表示有线程获取锁。**加锁时判断锁变量的值，根据锁变量值来判断能否加锁成功；释放锁时需要把锁变量值设置为0，表明客户端不再持有锁**。

​	单节点redis实现分布式锁

加锁的操作包含三个操作(读取锁变量、判断锁变量值以及把锁变量值设置为1)。可使用lua脚本和redis单命令操作。

redis单命令操作：

+ SETNX和DEL命令组合来实现加锁和释放锁。

+ 并且给锁变量设置一个过期时间。解决有客户端持锁后异常，然后无法释放锁

+ 加锁时客户端给锁变量设置成一个唯一值。

  ```
  //nx表示不存在才设置，ex和px过期时间 
  SET key value [EX seconds | PX milliseconds]  [NX]
  ```

+ 释放锁(释放锁操作的逻辑也包含了读取锁变量、判断值、删除锁变量)]

  使用lua脚本保证原子性



6. 分布式最终一致性

   不会

7. 负载均衡

   不会

8. 长链接和短连接

   

9. 负载均衡

   

10. redis 高可用方案

    

    